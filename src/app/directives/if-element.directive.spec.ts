/* import { IfElementDirective } from './if-element.directive';

describe('IfElementDirective', () => {
  it('should create an instance', () => {
    const directive = new IfElementDirective();
    expect(directive).toBeTruthy();
  });
});
 */